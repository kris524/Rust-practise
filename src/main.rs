
use rand::Rng;
// mod exercism1;


fn main() {
    let mut rng = rand::thread_rng();

    // let x: u8 = rng.gen();
    // let y: u32 = rng.gen_range(1..3);
    // println!("x = {}", x);
    // println!("y = {}", y);
    // exercism1::sublist_checker( &[1,2,3], &[1,2,3,4]);
}


#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2+2, 4)
    }
}

